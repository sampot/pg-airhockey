# 空氣曲棍球（`pg-airhockey`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-airhockey.yaml`

## 1. 一句話

拖曳／鍵鼠操作下半場球拍、彈盤進門的空氣曲棍球人機對戰——先得 7 分勝；致敬空氣曲棍球類型玩法，非任一商業作品復刻。

## 2. 定案速覽

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-airhockey` / `game` / `街機` |
| status | `listed` |
| 模式 | 單人 vs AI；單局制（無關卡） |
| 勝負 | 先得 **7** 分（`WIN = 7`）；最高連勝跨局保存 |
| 視角/畫布 | 直向俯視 320×520 邏輯畫布；手機直持優先 |
| 對手 AI | 內建單一難度（速度上限 200–340） |
| 素材 | 全部程式繪製＋WebAudio 合成音效（無圖檔資產） |
| 交付形 | 純 HTML＋CSS＋JS；無 build；`npx vitest run` 測試 |

## 3. 完整規則（現行實作）

### 3.1 目標與勝負

- 玩家守下方球門、AI 守上方球門；把彈盤打進對方球門得 1 分。
- 先達 7 分結束；玩家勝時更新最高連勝（敗北僅清零當前連勝，不動最佳值）。
- 得分後有 1.1 秒定格（`scoreFlash`），其間球拍仍可移動但彈盤凍結；之後由失分方開球。

### 3.2 場地與物理參數（實際值）

| 參數 | 值 | 意義 |
| --- | --- | --- |
| 畫布 | W=320, H=520 | 邏輯座標 |
| 球門寬 | GOAL=96 | 位於上下牆中央 `(W±GOAL)/2` |
| 牆厚 | WALL=4 | 反彈係數 0.92 |
| 彈盤半徑/上限速 | r=12 / 900 | 低於速度 8 視為靜止 |
| 摩擦 | ×0.992／步（120Hz） | 每 sim step 衰減 |
| 球拍半徑 | r=28（雙方） | 玩家擊球貢獻上限速 900 |
| 模擬步長 | 1/120s，≤8 substeps/frame | 保證 30fps 與 60fps 行為一致 |

### 3.3 操作與活動範圍

- 玩家球拍只能在 **下半場**（y ≥ H/2+r+4）；滑鼠移動即貼齊游標（免按住）、觸控拖曳、方向鍵/WASD 以速度 420 移動。
- 球拍移動掃掠分割（每段 ≤10px），快速甩拍不會穿過彈盤。
- 擊球衝量來自球拍實際位移速度（上限 900）；低速接觸時額外給法線推力 90 避免黏拍。

### 3.4 AI 行為

- 預設巡邏上場中央（y≈64±6 正弦擺動）。
- 彈盤進入上半場且朝 AI 球門運動（威脅）或慢速滾留（<90）時追擊：從球門側繞到彈盤後方再前推；速度上限一般 280，角落球與撿慢球 340。
- 角落卡球時先側移 44px 再沿牆推送；已重疊時直接把彈盤撥向中場+玩家半場。
- AI 待機動作由模擬時間驅動（不用 `performance.now`），測試可決定性重放。

### 3.5 邊界處理

- 卡死偵偵測：彈盤速度 <24 且停在欄角連續 18 步（約 0.15s）→ 朝中場方向施 140 衝量並稍微剝離牆面，避免永久卡角。
- 快速彈盤不會穿牆（位置鉗制在牆內再反射）；球門口以外才反彈上下牆。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 滑鼠移動（下半場） | 球拍 1:1 貼齊 |
| 觸控拖曳（下半場） | 球拍跟隨手指 |
| 方向鍵／WASD | 球拍速度控制（420/s） |
| 音效鈕 | 開/關（記住選擇） |
| 開局 | 開始／重開（非破壞性，不需確認） |

- HUD：雙方比分、訊息列（得分/失分/勝負）、當前連勝、最高連勝。
- Mobile-first：直向單欄；桌面加寬球桌。禁 `alert`／`confirm`／`prompt`。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| `pg-airhockey-win-streak` | 最高連勝（字串數字） | 開局載入讀取；玩家勝場後 PUT |
| `pg-airhockey-sfx` | `"1"`/`"0"` 音效開關 | 切換時寫 localStorage（僅偏好，非 KV） |

- 規則：`/api/kv` 為唯一權威；裸 `localStorage` 只能當輕量快取（現行 streak 即 LS 快取＋KV 回寫模式）。新增任何分數/進度一律走 `/api/kv/{key}`。

## 6. 美術／音效／署名

- 無外部圖像資產：球桌/球拍/彈盤全由 canvas 繪製；音效為 WebAudio 振盪器合成（`audio.js`），無需 ATTRIBUTION。
- 若未來加入素材：拷進 `assets/`、更新 `ATTRIBUTION.md`（CC0 也須署名）、同步 `sam-manifest.json` 的 files。

## 7. 測試（`npx vitest run`）

現有覆蓋（`game.test.js`，19 例）：球拍活動範圍與 1:1 貼齊、得分暫停仍跟隨、掃掠不穿穿盤、擊球速度上限、30/60fps 一致性、不穿牆、上下門得分、先到 7 分結束、AI 範圍、角落卡球解除（含無 AI 干預）、AI 決定性、重疊分離、極速鉗制。

改動規則/物理必補對應邊界測試；UI 不在測試範圍。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；確認一律頁內 UI。
3. Mobile-first；主操作不可 hover-only。
4. 分數/進度以 `fetch('/api/kv/…')` 為權威；禁止裸 `localStorage` 當權威。
5. 不自行載入 `sdk.js`；宿主注入 `window.PG`。本作未用 `PG.libs`（vanilla 即可，勿無故引入 Phaser）。
6. 改動可執行邏輯前先寫失敗測試（TDD）。
7. 檔案清單變動須同步 `sam-manifest.json`（下載契約）。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：強化手感與重玩誘因，不改變「即時物理對抗」的核心認同。

**高優先（直接提升手感）**

1. **AI 難度三段**（休閒/標準/兇猛）：調 `speedCap`（如 160/200–340/380–420）與追擊觸發閾值即可，風險低；解決現行單一難度「新手打不贏、老手贏太容易」。
2. **進球回饋升級**：進球瞬間短震動（`navigator.vibrate`，Android Web 支援）、球網閃光、比分跳動縮放；失分/得分音色區分更明顯。
3. **開球多樣性**：目前開球方向固定朝發球方對側；可加隨機角度微偏移與偶發「中路快球」，減少開場節奏重複感。

**中優先（深度與重玩性）**

4. **同機雙人**（上方玩家觸控上半場）：硬體零成本增加社交玩法；需把 AI 區塊抽換成第二輸入源，注意上下半場輸入隔離與測試。
5. **賽制變體**：先得 7／計時 90 秒高分／生存模式（丟分 3 次出局）三選一；共用同一物理核心，只換終局條件。
6. **里程碑稱號**：連勝 3／5／10 顯示稱號 toast（如「三連勝！」），KV 存歷史最高稱號等級。

**低優先（打磨）**

7. 彈盤拖尾粒子（依速度強度）、牆撞擊火花；力度影響音高（現行 osc freq 已參數化，易做）。
8. 戰績頁：總場次/勝率/最長拉鋸比分，存 KV 單一 JSON key。
